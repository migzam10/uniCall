# Reverse proxy (despliegue)

Aquí irá la configuración de Nginx como reverse proxy para el backend
(HTTPS/WSS, sección 19) cuando el proyecto se despliegue en un servidor
propio. No requerido para desarrollo local.
